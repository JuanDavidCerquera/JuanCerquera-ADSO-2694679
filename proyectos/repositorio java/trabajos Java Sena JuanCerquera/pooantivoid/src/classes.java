public class classes {

    public double calcularRadioDelCirculo(double radio) {
        double area = Math.PI * (Math.pow(radio, 2));
        return area;
    }
    /*
     * public String ejemploReturn(){
     * if(true){
     * return "hola";
     * }else{
     * String texto = "que tal";
     * return texto;
     * }
     * }
     */
}
